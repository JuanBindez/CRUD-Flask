# CRUD-Flask

## CRUD with flask


### Create Database (if not exist)
    sqlite3 instance/database.db < database.sql

### requirements
    pip install -r requirements.txt

### run
    python3 app.py
    